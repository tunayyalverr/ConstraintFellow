import XCTest
@testable import ConstraintFellow

final class ConstraintFellowTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ConstraintFellow().text, "Hello, World!")
    }
}
